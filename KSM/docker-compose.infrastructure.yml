services:
  postgres:
    image: postgres:18.3-alpine
    container_name: iwm-postgres
    environment:
      POSTGRES_DB: iwm
      POSTGRES_USER: iwm
      POSTGRES_PASSWORD: iwm
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U iwm -d iwm"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    volumes:
      - iwm-postgres-data:/var/lib/postgresql
    networks:
      - iwm

  redis:
    image: redis:8.6.2-alpine
    container_name: iwm-redis
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 5s
    volumes:
      - iwm-redis-data:/data
    networks:
      - iwm

  kafka:
    image: confluentinc/cp-kafka:8.2.0
    container_name: iwm-kafka
    ports:
      - "9092:9092"
      - "29092:29092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:29093
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:29092,PLAINTEXT_HOST://0.0.0.0:9092,CONTROLLER://0.0.0.0:29093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
    healthcheck:
      test: ["CMD-SHELL", "kafka-topics --bootstrap-server localhost:9092 --list >/dev/null 2>&1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 30s
    networks:
      - iwm

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:9.3.3
    container_name: iwm-elasticsearch
    environment:
      discovery.type: single-node
      xpack.security.enabled: "false"
      xpack.security.http.ssl.enabled: "false"
      ES_JAVA_OPTS: -Xms512m -Xmx512m
    ports:
      - "9200:9200"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:9200/_cluster/health | grep -E '\"status\":\"(yellow|green)\"' >/dev/null"]
      interval: 15s
      timeout: 10s
      retries: 20
      start_period: 40s
    volumes:
      - iwm-elasticsearch-data:/usr/share/elasticsearch/data
    networks:
      - iwm

  redis-exporter:
    image: oliver006/redis_exporter:v1.82.0
    container_name: iwm-redis-exporter
    depends_on:
      redis:
        condition: service_healthy
    command:
      - --redis.addr=redis://redis:6379
    ports:
      - "9121:9121"
    networks:
      - iwm

  elasticsearch-exporter:
    image: quay.io/prometheuscommunity/elasticsearch-exporter:v1.10.0
    container_name: iwm-elasticsearch-exporter
    depends_on:
      elasticsearch:
        condition: service_healthy
    command:
      - --es.uri=http://elasticsearch:9200
    ports:
      - "9114:9114"
    networks:
      - iwm

  prometheus:
    image: prom/prometheus:v3.11.2
    container_name: iwm-prometheus
    depends_on:
      redis-exporter:
        condition: service_started
      elasticsearch-exporter:
        condition: service_started
    ports:
      - "9090:9090"
    volumes:
      - ./ops/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./ops/prometheus/alerts:/etc/prometheus/alerts:ro
      - iwm-prometheus-data:/prometheus
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - iwm

  grafana:
    image: grafana/grafana:12.4.2
    container_name: iwm-grafana
    depends_on:
      prometheus:
        condition: service_started
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
    ports:
      - "3000:3000"
    volumes:
      - ./ops/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./ops/grafana/dashboards:/var/lib/grafana/dashboards:ro
      - iwm-grafana-data:/var/lib/grafana
    networks:
      - iwm

volumes:
  iwm-postgres-data:
  iwm-redis-data:
  iwm-elasticsearch-data:
  iwm-prometheus-data:
  iwm-grafana-data:

networks:
  iwm:
    name: iwm-network
